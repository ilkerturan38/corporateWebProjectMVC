@using PagedList;
@using PagedList.Mvc;
@using KurumsalWeb.Helper; <!--blogDetay sayfasının SeoURL işlemleri için Proje içerisindeki Helper Klasörünü seçtik.-->
@using KurumsalWeb.Models.Model.Entity
@model PagedList.IPagedList<blog>
@{
    ViewBag.Title = "Blog";
    Layout = "~/Views/Shared/_HomeLayoutGuncel.cshtml";
}



<section id="inner-headline">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <ul class="breadcrumb">
                    <li><a href="#"><i class="fa fa-home"></i></a><i class="icon-angle-right"></i></li>
                    <li class="active">Blog</li>
                </ul>
            </div>
        </div>
    </div>
</section>

<section id="content">
    <div class="container">
        <div class="row">
            <div class="col-lg-8">
                @foreach (var item in Model)
                {
                    <article>
                        <div class="post-image">
                            <div class="post-heading">
                                @*href kısmı önceden Home/BlogDetay(home içerisindeki blogDetay View'dı).SeoURL ile ismini değiştirdiğimiz için Blog/SeoHelper şeklinde değiştirdik. *@

                                <h3><a href="/blog/@SeoHelper.ToSeoUrl(item.Baslik).ToLower()-@item.blogID">@item.Baslik</a></h3>

                                @*<h3><a href="/_Home/BlogDetay?ID=@item.blogID">@item.Baslik</a></h3>*@  <!--SeoURL uyumsuz QueryString'li İşlem-->
                            </div>
                            <img src="@item.resimURL" alt="@item.Baslik" width="120" height="80" />
                        </div>
                        <p>
                            @Html.Raw(item.Icerik.Substring(0, item.Icerik.Substring(0, 150).LastIndexOf(" ")))... @*İçerik Kısmının Sadece İlk 150 Karakteri gözüksün *@
                        </p>
                        <div class="bottom-article">
                            <ul class="meta-post">
                                <li><i class="icon-folder-open"></i><a href="#">@item.Kategoriler.kategoriAdi</a></li>
                            </ul>
                            <a href="/_Home/BlogDetay/@item.blogID" class="pull-right">Devamını Oku..<i class="icon-angle-right"></i></a>
                        </div>
                    </article>
                }
                <div id="pagination">
                    @Html.PagedListPager(Model, page => Url.Action("blog", new { page }), PagedListRenderOptions.Classic)
                </div>
            </div>
            <div class="col-lg-4">
                <aside class="right-sidebar">
                    <div class="widget">
                        <form class="form-search">
                            <input class="form-control" type="text" placeholder="Search..">
                        </form>
                    </div>
                    <div class="widget">
                        <h5 class="widgetheading">Kategoriler</h5>
                        @Html.Action("blogKategoriPV", "_Home")
                    </div>
                    @Html.Action("BlogSonKayitlarPV", "_Home")
                </aside>
            </div>
        </div>
    </div>
</section>

